# docs/09 BUG 修正後：待辦清單（2026-09-13）

> **現況註記（2026-09-14）**：本文件是當時的部署／重建清單，不是現行功能總表。docs/13 的三項程式修正已由 `8d7df2c`、`5008423`、`9e2c63a` 完成；MOPS 後續見 docs/14、docs/15。下方「瀏覽器實際點過的 UI」仍是有效的人工驗收清單。是否已完成 production 重建與 OPS key 設定，應以目標環境的 migration、`job_run` 與 `/system` 現況確認，不可只靠本文件推定。

docs/09 審閱報告的 15 項 BUG + RISK-01 已分四批修正並 commit；複查建議書 docs/12 的 Phase 1–6 與 §10
也已完成（見文末「已完成」）。本文件整理**需要人處理或決定**的事項，依緊急程度排序。

> **重要**：線上先前那次全量重建是在 docs/12 修正**之前**跑的，評分語意（同日大盤、子項分組、CVD slope
> 改名）已再改變，**部署本輪後需要再全量重建一次**。

---

## 一、部署到線上前必做（否則會壞）

### 1. 線上主機設定 `OPS_API_KEY`

第 4 批起，回補端點經 nginx / tailscale 轉發時**沒有金鑰一律 403**。線上
（vultr.tail663489.ts.net）系統頁的「回補」按鈕會直接失效。

```bash
# 產生金鑰
python -c "import secrets;print(secrets.token_urlsafe(32))"
```

- systemd 部署：填進專案根目錄 `.env` 的 `OPS_API_KEY=`，`sudo systemctl restart twchip-api`
- Docker 部署：填進 compose 用的 `.env`（`docker-compose.yml` 已接好 `OPS_API_KEY`）
- 之後在系統頁第一次按回補時輸入金鑰（只存在該分頁 sessionStorage）

說明已寫入 `deploy/tailscale.md`「回補端點金鑰（必設）」、`deploy/env.example`、`deploy/docker/env.example`。

### 2. 依改動範圍升級（本次屬「改評分邏輯 + 新增 migration」）

| 部署方式 | 步驟 |
|---|---|
| Docker | `git pull && docker compose up -d --build`（entrypoint 自動 `alembic upgrade head`）→ 第二節全量重建 |
| systemd | `git pull` → `APP_ENV=prod .venv/bin/alembic upgrade head` → 前端 `pnpm build` → `restart twchip-api` / `twchip-web` → 第二節全量重建 |

- 新 migration（依序，entrypoint / `alembic upgrade head` 會全部套用）：
  - `c3f1a9d2e4b7`：`feature_daily.resistance_high`、`is_limit_locked`
  - `d4a7b1c9e2f3`：`threshold_override`、`threshold_change`、`signal_snapshot.config_version`
  - `e5b8c2d0f4a6`：`job_run`
  - `f6c9d3e1a5b7`：`feature_daily.intraday_obi` → `cvd_slope_norm`（值保留）
  - `a7d0e4f2b6c8`：`signal_snapshot` / `daily_price` / `corporate_action` 的 `updated_at`
- 無新增 Python / npm 相依套件
- 參考：`deploy/docker.md`「升級流程（SOP）」、`deploy/systemd.md`

---

## 二、全量重建歷史資料（dev 與線上都要）

**現況不一致**：dev 庫只有 2026-09-08 一天的 `feature_daily` 用新算法重建過，其餘日期與全部
`signal_snapshot` 仍是舊算法。修正前後分數不可比，驗證頁數字目前仍反映舊分數。

```bash
APP_ENV=dev python -m scripts.rebuild_signals        # 覆寫全部 feature_daily + signal_snapshot
```

- 約 130 個交易日，預估 10 多分鐘
- 會覆寫既有 `feature_daily` 與 `signal_snapshot`（可重跑，冪等）
- 線上：`APP_ENV=prod`；Docker 用 `docker compose exec api python -m scripts.rebuild_signals`

### 重建後驗收

1. `/validation` 頁：看 IC 與 **NW t**（已改為 Newey–West；樸素 t 另列僅供對照）
2. `APP_ENV=dev python -m scripts.score_monotonicity`：分數 bucket 是否單調、MAE 是否惡化
3. 結果寫回 `CLAUDE.md`「現況／已知限制」段（§28 成功標準是否有進展）

---

## 三、參數決定（已定案 2026-09-13）

| 參數 | 決定 | 依據（結構推導，非回測挑選） | dev 2026-09-08 影響 |
|---|---|---|---|
| `risk.resistance_lookback_bars` | 60 → **20** | 與最長持有期 20D、MA20 同尺度；60 日前高常在持有期內不可達 | ≥75 分者 RR≥2 比例 68% → 20% |
| `risk.breakout_policy` | 新增，**watch** | 突破股 RR ≈ k÷1.65 近乎常數，任何 `breakout_target_atr` 都讓 gate 失去鑑別力 | 29 檔突破股改 WATCH |

合計 BUY 62 → 22。之後可做「價格-only 目標可達率」檢查（前半觀察、後半驗證）確認目標合理，但不以它挑讓 IC 最好的參數。

---

## 四、要在瀏覽器實際點過的 UI（目前只做過 tsc / lint / API 驗證）

- [ ] 個股頁「我持有這檔？」表單：輸入成本/停損 → 顯示 HOLD/REDUCE/EXIT、「清除」連結、已達 TP 的說明
- [ ] 系統頁回補：設了 `OPS_API_KEY` 時第一次按回補出現金鑰輸入框、輸入後可觸發
- [ ] 系統頁回補結果：顯示「有資料 / 無資料 / 失敗」與「降級來源」
- [ ] 驗證頁 IC 顯示「NW t=…，樸素 …」
- [ ] 個股頁盤中資金流面板標籤「單檔分數·非 Chip Score」
- [ ] 個股頁 RR 旁顯示目標依據（前高壓力 / 突破後 ATR 推估 / 無壓力位資料）
- [ ] 持倉表單：停損設在成本以上時 TP 留空並顯示說明
- [ ] `/settings` 設定頁：儲存、重設、鎖定權重解鎖、待重建提示與「全量重建分數」按鈕
- [ ] `/system` 工作面板：選腳本、填參數、執行、看輸出、取消

驗證建議用 `MODE=prod ./scripts/dev.sh`（dev 模式 HMR 在部分環境會卡 hydration）。
開發模式若跳出「Failed to connect to MetaMask」浮層，是瀏覽器擴充功能的錯誤，與本專案無關。

---

## 五、行為變更（知悉即可，非 bug）

| 變更 | 影響 |
|---|---|
| BUY 數量大減 | dev 2026-09-08：145 → 114（RR/漲停 gate）→ 57（外資因子恢復作用，前 25% 名單約換 200 檔） |
| 外資因子原本失效 | 舊算法單一標的外資 z=44.1 把其餘壓在 \|z\|<0.3；修正後 \|z\|>1 有 475 檔。**這可能是歷史 IC≈0 的原因之一，重建後需重驗** |
| 驗證頁顯著性下修 | 舊分數 20 日 IC：樸素 t=2.31（會標「✓有效」）→ NW t=1.50（不顯著） |
| `/ticks`、`/orderflow` | Shioaji 抓取失敗改回 502（原本回空資料偽裝成「無逐筆」） |
| `/orderflow` 單檔分數 | 權重改用 config `weights.intraday`，數值會與以前不同 |
| 資料不足存 NULL | 新上市/停牌復牌股的 ma20、atr14、法人 5 日等為 NULL；institutional 子項全缺時排除成分（dev 當日 19 檔） |
| EOD 降級 | TAIEX / TDCC / TPEx 各端點失敗不再中止整個 EOD，失敗來源列在排程結果 `degraded` |
| 回測 | 停牌後復牌日不再視為進場；9 支 `scripts/` 回測與 OOS 腳本同步，重跑結果會變 |

---

## 六、選擇性後續（未排入）

- **前端 lint 歷史項目已清除**：2026-09-14 本機 `pnpm lint` 與 `pnpm exec tsc --noEmit` 通過；production build 仍應在可連字型來源的正常環境驗收
- **其他 `/api/*` 唯讀端點與 `GET /api/ops/status` 無認證**：唯讀、已有快取與單飛；若不想讓 tailnet 內所有裝置看到可再加
- **`/flows` 還原邏輯**：仍寫在路由內，與背離掃描共用的是底層 `cumulative_factors`，不是同一個 service 函式

---

## 已完成（commit）

| commit | 內容 |
|---|---|
| `33d46be` | 第 1 批：RR 改用壓力位、Strong Bear 用原始趨勢、鎖死漲停接線、持倉參數（BUG-01～04） |
| `06e7834` | 持倉模式已越過的 TP 留空並說明 |
| `16aa838` | 第 2 批：EOD 各來源降級、逐筆狀態區分、視窗不足與缺資料存 NULL（BUG-07～11） |
| `cc88262` | 第 3 批：離群值截尾、breadth 還原、前瞻驗證同口徑 + NW t、背離掃描、盤中分數權重、回測交易日曆（BUG-05/06/12～15） |
| `06d9f33` | 第 4 批：回補端點認證與稽核 log（RISK-01） |
| `5f484d2` | 設定改存 DB 覆寫、signal_snapshot 記錄設定版本 |
| `101ac1c` | 系統頁白名單腳本按鈕（子行程、紀錄、取消） |
| `b0dd052` | 前端：`/settings` 設定頁、系統頁工作面板 |
| `a70f2d9` | docs/12 A：同日大盤上下文、institutional 子項可用性分組（Phase 1–2） |
| `b21513e` | docs/12 B：出貨警示預設關閉、假 OBI 改名 CVD slope、移動停損 TP（Phase 3–4） |
| `f459d30` | docs/12 C：前瞻驗證快取依 updated_at 失效、pending 定義（Phase 5） |
| `1505922` | docs/12 D：剩餘策略/統計視窗 config 化（Phase 6） |
| `4901f86` | 逐筆同 ts 以寫入序排序（盤中特徵原本非決定性） |
| `900e031` | RR 目標依據標示（docs/12 §10） |

測試：312 passed。
